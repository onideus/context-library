services:
  context-library:
    build: .
    container_name: context-library
    restart: unless-stopped
    ports:
      - "127.0.0.1:3100:3100"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
